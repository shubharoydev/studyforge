VITE_API_URL=/api
# Uploads go DIRECTLY to the backend to avoid the Vite dev proxy
# buffering large multipart bodies (fixes uploads stalling ~59%).
# In production, point this at your backend API base URL without a trailing slash.
VITE_UPLOAD_URL=http://localhost:4000/api
